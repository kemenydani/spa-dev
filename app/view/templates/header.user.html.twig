<div class="user-area">
    {% if isLogged %}
        <div>
            <a href="/profile/{{ AuthUser.getUsername() }}" class="username">{{ AuthUser.getUsername() }}</a>
            <div class="logout"><a href="/logout">logout</a></div>
        </div>
        <div class="profile-picture picture-md circle bordered my-profile-picture" style="background-image: url({{ AuthUser.requestProfilePicture() }})"></div>
    {% else %}
        <a class="signin" href="/auth">Sign In</a>
    {% endif %}
</div>